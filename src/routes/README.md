# Routes

- `/` — landing (hero, upload, analysis-mode picker, processing)
- `/auth` — email/password + Google (public)
- `/research`, `/about`, `/contact` — public content pages
- `/_authenticated/dashboard` — command-center dashboard for latest analysis + history
- `/_authenticated/reports` — saved-report archive
